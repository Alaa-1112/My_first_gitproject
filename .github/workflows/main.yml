on:
  push:
    branches:
      - master




jobs:
  build:
    runs-on: ubuntu-latest


    steps:
      - name: ' before printing folder'
        run: ls

      - uses: actions/checkout@v3

      - name: ' after printing folder'
        run: ls